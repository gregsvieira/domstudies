// Criando e Adicionando elementos no HTML

// creatElement
   // const div = document.createElement('div');//mesmo não vista está criada em memória e existe
   // div.innerText = "Olá Devs!"

    // só que ainda não é visto em página, precisa ser adicionado:

    // append prepend
    // append = adicionar algo depois
            //seleciona o local:
 //       const body = document.querySelector('body'); 
        //body.append(div) //se adiciona o elemento após o local

        //prepend adiciona antes
   //     body.prepend(div) //se adiciona o elemento antes o local
